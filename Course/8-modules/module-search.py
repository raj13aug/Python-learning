import sales
import sys

print(sys.path)

# sales.py doesn't find this file, it will look in a bunch of predefined directories that come with python installations.

# import items search these directories to find this module if finds the module, our search stops
